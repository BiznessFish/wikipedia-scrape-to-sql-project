CREATE TABLE IF NOT EXISTS "atlantic_hurricanes" (
"index" INTEGER,
  "year" INTEGER,
  "tropical_storms" INTEGER,
  "hurricanes" INTEGER,
  "major_hurricanes" INTEGER,
  "deaths" DECIMAL(10,5),
  "damage" DECIMAL(10,5),
  "notes" TEXT
);
